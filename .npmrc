# For anyone who runs plain npm here: refuse to install on a Node version outside
# package.json "engines" instead of failing later in stranger ways.
# pnpm 11 does not take pnpm settings from this file - its copy of this rule is
# engineStrict in pnpm-workspace.yaml.
engine-strict=true
